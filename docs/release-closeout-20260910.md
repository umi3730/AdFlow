# 版本整理与验证 · 2026-09-10

本次整理此前已完成的控制台拆分、页面状态恢复、真实事件计量核对、浏览器回归与诊断修复，并将当前代码和说明统一交付。未新增竞价算法、迁移或容量实验。

## 交付内容

- 控制台按工作区拆分组件与数据 Hook；页面和已应用筛选支持刷新及历史恢复。
- 批量投放结果区分请求结束、事件受理和实际计量；使用原始事件 ID 核对最近最多 20 轮结果。
- 桌面与窄屏浏览器回归使用独立 Go 内存实例及前端副本，纳入 GitHub Actions。
- 诊断正确解释历史决策中的 `no_creative`，提示补充素材后使用新的请求 ID，保留历史结果。
- 架构及并发说明统一为 MySQL 8.0、结算每批 4 条和 15 秒租约；分别说明预算/频控预占与曝光时一次 Lua 确认的原子边界。
- 开发说明区分 `revision` 与发布 `version`，并明确测试包范围、测量口径及尚未验证的集群扩展方向。

## 本地验证

| 检查 | 结果 |
| --- | --- |
| `scripts/verify.ps1` | Go 格式、vet、正式包测试、前端 lint 与 145 项测试通过；Go 测试使用可复用缓存 |
| TypeScript | `npm exec -- tsc --noEmit` 通过 |
| 前端格式 | 175 个文件检查通过 |
| 依赖审计 | `npm audit --audit-level=high`：0 个漏洞 |
| 生产构建 | `npm run build` 通过 |
| 浏览器回归 | 桌面 9 项、390px 窄屏 9 项，合计 18 项通过，约 3.8 分钟 |
| 文档与补丁 | 修改文档的本地链接有效，`git diff --check` 通过 |

原始输出：[工程检查](verification/release-closeout-20260910/verify.log) · [生产构建](verification/release-closeout-20260910/build.log) · [浏览器回归](verification/release-closeout-20260910/browser.log)。

本次浏览器回归使用隔离的内存/同步模式和 mock 模型，没有修改日常 MySQL/Redis/Kafka 数据，也不形成新的吞吐结论。竞态检测由提交后的 CI 再检查；真实基础设施与容量的历史证据继续按各自测试版本阅读。
