# 前端调整记录（2026-09-07）

## 布局与标识

- 保留蓝白与粉色点缀，缩小圆角，移除总览装饰横幅、光晕和独立悬浮指标卡。
- 总览采用连续指标栏；广告计划列表全宽，搜索合并，新建计划改为弹窗，保留 Agent 带入规则。
- 各页面复用 PageHeading；侧栏改为 AdFlow 文字标识与「广告决策平台」副标题。

## 中文标签与提示清理

- 画像列表、编辑器、决策详情和定向摘要共享中文标签字典，包含演示用户、演示排除人群、数码兴趣等；底层标签 ID 不变。
- 编辑使用标签块，支持移除、常用标签切换、自定义标签输入及回车添加。中文常用名称解析为原有标识，已保存的自定义标签不重命名。
- 标签筛选支持中文名称；设备筛选使用字典下拉，列表和模拟用户行使用设备显示名称。
- 按用户要求移除模拟页常驻黄色说明和普通蓝色成功提示；保留实际错误及部分数据未加载等必要反馈。

中文标签回归后 89 项前端测试通过。浏览器验证「数码兴趣」筛选正确返回对应已保存画像；本次显示调整没有改写后端数据。
